% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resmush_url.R
\encoding{UTF-8}
\name{resmush_url}
\alias{resmush_url}
\title{Optimize online image files}
\usage{
resmush_url(
  url,
  outfile = file.path(tempdir(), basename(url)),
  overwrite = FALSE,
  progress = TRUE,
  report = TRUE,
  qlty = 92,
  exif_preserve = FALSE
)
}
\arguments{
\item{url}{A character vector of URLs pointing to hosted image files. The API
can optimize PNG, JPEG, GIF, BMP and TIFF files.}

\item{outfile}{A character vector of paths where optimized files are stored.
By default, files are created in \code{\link[=tempdir]{tempdir()}} with the same \code{\link[=basename]{basename()}} as
each file in \code{url}. \code{outfile} must have the same length as \code{url}.}

\item{overwrite}{Logical. Should existing files in \code{outfile} be overwritten?
If \code{FALSE}, existing paths are made unique with a numeric suffix, such as
\code{example_01.png}.}

\item{progress}{Logical. Should a progress bar be displayed?}

\item{report}{Logical. Should a summary report be displayed in the console?}

\item{qlty}{An integer between \code{0} and \code{100} indicating the JPEG quality
level. For best results, use values above \code{90}. This argument only
affects JPEG files.}

\item{exif_preserve}{Logical. Should
\href{https://en.wikipedia.org/wiki/Exif}{EXIF} metadata be preserved? The
default is \code{FALSE}. This removes it.}
}
\value{
An invisibly returned data frame with one row per result and columns
containing source and destination paths, formatted and raw file sizes,
compression ratios and status notes. Returns \code{NULL} if no result is
available. Successful API calls also write the optimized files to disk. If
\code{outfile} contains duplicate paths, \code{resmush_url()} makes them unique with
suffixes such as \verb{_01} and \verb{_02}.
}
\description{
Optimize and download one or more online image files with the
\href{https://resmush.it/api/}{reSmush.it API}. The API is free for personal use
and accepts files smaller than 5 MB.
}
\examples{
\dontshow{if (httr2::is_online()) withAutoprint(\{ # examplesIf}

\donttest{
# Define the base URL.
base_url <- "https://raw.githubusercontent.com/dieghernan/resmush/main/inst/"

png_url <- paste0(base_url, "/extimg/example.png")
resmush_url(png_url)

# Optimize multiple URLs.
jpg_url <- paste0(base_url, "/extimg/example.jpg")

summary <- resmush_url(c(png_url, jpg_url))

# Inspect the returned optimization summary.
summary

# Display the PNG output.
if (require("png", quietly = TRUE)) {
  my_png <- png::readPNG(summary$dest_img[1])
  grid::grid.raster(my_png)
}

# Adjust the JPEG quality level.
resmush_url(jpg_url)
resmush_url(jpg_url, qlty = 10)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=resmush_clean_dir]{resmush_clean_dir()}} removes output files created by previous runs.
\item The \href{https://resmush.it/api/}{reSmush.it API documentation} describes the
external service.
}

Other image optimization functions:
\code{\link[=resmush_dir]{resmush_dir()}},
\code{\link[=resmush_file]{resmush_file()}}
}
\concept{optimize}
